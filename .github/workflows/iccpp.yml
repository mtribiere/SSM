name: C/C++ CI

on:
  push:
    branches: [ V2 ]
  pull_request:
    branches: [ V2 ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make clean && make
